# LucidTops blockchain container dependencies.
# OS-native (must be installed on the image — listed here so they are not assumed present):
#   - ca-certificates
#   - curl
#   - iproute2
#
# Pip packages:
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pymongo>=4.6.0
