# this is the Dockerfile to create the blockchain container for the LucidTops system.
# this is the core blockchain container for the LucidTops system.
# this container is called via the master server, NodeUser, AdminUser, MasterClassUser, and User.
# this container will be used to store the blockchain data and ledger data referenced by the blockchain system.
