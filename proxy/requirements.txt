# LucidTops Proxy container — pip deps for proxy/*.py modules only.
# Used by: ProxyRoutes (fastapi/httpx), RunProxy+Bootstrap (uvicorn),
# Clearnet-package (PySocks/socks). Stdlib-only modules need nothing here.
#
# OS-native (supplied at time of operation via APT_PACKAGES build-arg — do not assume present):
#   resolve from hardware pull / Bootstrap (e.g. tor, nginx, and related packages as required)
#
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
httpx>=0.27.0,<1.0.0
PySocks>=1.7.1,<2.0.0
