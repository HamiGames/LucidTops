# this is the Dockerfile to create the *.onion address useable webpage written in javascript.
# all pages and content will be stored in the /Frontend/webpage directory.
# the webpage will be served via the nginx server.
# the webpage will be served via the *.onion address.
# the webpage will be served via the nginx server.