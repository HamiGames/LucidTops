/* 
this is the visual interface for the remote view of the session system (sessionID is active)
all remote view controls will be set via the session control settings (settings.js)
the remote view is the window used for a peer to view the other peer's console via the session system
the remote view will only be accessible to the peer that is viewing the other peer's console
remote view:
- the remote view is used to display the remote view of the session system
- the remote view will display the remote view of the session system
- the remote view will display the remote view of the session system
- all the use of mouse and keyboard controls will be set via the session control settings (settings.js)
schema:
modern and clean with a focus on user experience and ease of use

*/
