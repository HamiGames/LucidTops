# LucidTops Rdp container — pip deps for Rdp/*.py modules only.
# Used by: RdpRoutes (fastapi), RunRdp (uvicorn), ScreenShare (mss/opencv),
# Mouse/keyboard (pynput), FileShare (aiofiles), socks/stem for Tor paths.
#
# OS-native (supplied at time of operation via APT_PACKAGES build-arg — do not assume present):
#   resolve from hardware pull / createRDP (display, audio, and related packages as required)
#
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
httpx>=0.27.0,<1.0.0
mss>=9.0.0,<11.0.0
opencv-python-headless>=4.8.0,<5.0.0
pynput>=1.7.6,<2.0.0
aiofiles>=23.0.0,<25.0.0
PySocks>=1.7.1,<2.0.0
stem>=1.8.0,<2.0.0
python-socks[asyncio]>=2.4.0,<3.0.0
